class Solution:
    def majorityElement(self, arr):
        candidate = None
        count = 0

        # Boyer-Moore Voting Algorithm
        for num in arr:
            if count == 0:
                candidate = num
                count = 1
            elif num == candidate:
                count += 1
            else:
                count -= 1

        # Verify candidate
        if arr.count(candidate) > len(arr) // 2:
            return candidate

        return -1