class Solution:
    def missingNum(self, arr):
        n = len(arr) + 1
        
        # Sum of first n natural numbers
        total = n * (n + 1) // 2
        
        # Sum of array elements
        arr_sum = sum(arr)
        
        # Missing number
        return total - arr_sum